# Session Handover — 2026-04-20 (evening)

This is the second handover of 2026-04-20. The morning session (handover
at `docs/journal/2026-04-20_handover.md`, resume id
`eaf5e53b-25fe-4dd9-8ae0-3d326485b4b7`) closed with P8.9 pending. This
session opened against that handover prompt and took CS4G from
"validation planned" through to "full production mocks on disk in a
dataset-agnostic layout", merged everything into `main`, and stopped
short of Phase 10 at the user's request.

## Goal

User's final instruction: conclude development for the day. The
`isoster` end (a separate repo that will consume these mocks) still has
prep work before the real benchmark can run; user has private plans and
references for Phase 10 that were explicitly out of scope.

Immediate-state goal for the next session: **do nothing until the user
gives instruction.** Read this file, catch up, wait.

## Completed This Session

### P8.9 — CS4G validation against Salo+2015 S4G subcomps cubes
- New script: `inputs/cs4g/scripts/qa_mockgal_vs_s4g.py` (~360 lines).
  Renders NGC1433 / NGC0275 / NGC1357 via mockgal libprofit at
  Spitzer/IRAC1 native settings (0.75"/pix, zp=21.097, Gaussian PSF
  FWHM=1.66") and compares against the IRSA-hosted `*_subcomps.fits`
  cubes.
- Pre-fetched the 3 cubes + the 3 composite PSFs from the IRSA S4G P4
  directory into `output/cs4g_s4g_irac1_test/{name}/` (gitignored).
- Shape correlation 0.88–0.9996 across all three. Commit `4aa8478`.

### P8.11 — Undo i-band → 3.6 μm color shift in P8.9 QA script
- First diagnosed the 1.31–1.64× flux excess as a cosmology round-trip
  bug. That was **wrong**. Real cause was a band mismatch:
  `cs4g_sample_models.yaml` stores predicted-i-band abs_mag (because
  the production path wants HSC-i), but P8.9 validates against
  IRAC1 3.6 μm. Fix: read the per-galaxy `color_shift_3p6_minus_i`
  (already stored in the YAML) and add it to each component's
  `abs_mag` before rendering against the IRAC1 reference. 3-line
  change in `qa_mockgal_vs_s4g.py:build_mockgalaxy`.
- Post-fix flux ratios: NGC1433 0.996, NGC0275 0.991, NGC1357 0.989.
  Peak ratios: 1.96 / 1.01 / 3.98 (bulgeless NGC0275 ≈1; remaining
  >1 is the Gaussian-vs-composite PSF mismatch, deferred as P8.12).
- `docs/LESSON.md` rewritten to document the band convention as the
  durable lesson (replacing my initial wrong cosmology-round-trip
  writeup). Commit `3365db3`.

### P8.10 — Manifest `defaults:` merging in `load_image_configs`
- `mockgal.load_image_configs` now merges a top-level `defaults:` dict
  into each `configs[]` entry (per-row keys win). Unknown top-level
  keys (`run_name`, `model_file`, `output_root`, `description`) are
  silently ignored so one manifest can drive both `python -m mockgal
  --config ...` and higher-level runners.
- Malformed (non-mapping) `defaults:` raises `ValueError`.
- New test class `tests/test_mockgal.py::TestLoadImageConfigs` with
  6 cases (merge, override, unknown-keys, legacy `image_configs:`,
  malformed default).
- The Huang2013 runner was unaffected (it calls `resolve_run_manifest`
  / `build_image_config` locally — never reaches
  `load_image_configs`). Commit `9680f4c`.

### P8.12 — Deferred
- User explicitly deferred the composite-PSF follow-up ("can live
  without worrying about it given the evidence"). The composite PSF
  FITS files are still pre-fetched locally for future use.

### Phase 9 — Production HSC-i renders (198-galaxy CS4G sample)

- **C9.1**: `git mv inputs/huang2013/scripts/generate_huang2013_mocks.py
  scripts/generate_mocks.py`. Dropped hardcoded `output_base /
  "huang2013" / galaxy_name`; `--output` is now the dataset root
  directly. Removed host-specific `libprofit_path_note` from
  `run_metadata.json`. Renamed `tests/test_generate_huang2013_mocks.py`
  → `tests/test_generate_mocks.py`. Regression-checked end-to-end
  with `IC 1459` clean_z005 — output layout byte-identical to
  existing Huang2013 production data. Commit `9e9f6d0`.
- **C9.2**: `inputs/cs4g/runs/cs4g_hsc_i_wide.yaml` (5 configs:
  `clean_z005`, `wide_z{005,020,035,050}`) and
  `cs4g_hsc_i_dud.yaml` (4 configs: `deep_z{005,020,035,050}`)
  mirror Huang2013 HSC-i defaults exactly except `size_factor: 4.0`
  (vs Huang2013's 6.0). Size-factor rationale: CS4G is disk-
  dominated (n=1 disks always anchor the max-Re auto-sizing), so
  4×Re captures 98.6% of disk flux and ~100% of bulge + Ferrer bar
  flux — exceeds Huang2013's effective ~88% completeness for n=4
  de Vaucouleurs at 44% of the pixel footprint. Commit `ace938e`.
- **C9.3** (smoke, within C9.2 commit): 3 galaxies × both manifests →
  byte-identical layout to `~/Dropbox/work/data/huang2013/`; 6.7 s
  per 3×5 configs + 5.1 s per 3×4 configs.
- **C9.5**: `docs/SPEC.md` created as architectural source of truth.
  First section documents the mock-dataset folder contract
  (per-galaxy subdir with 9 FITS + 1 mosaic + 3 root metadata
  files, multi-manifest overwrite semantics, required FITS header
  keys, required metadata fields, two current conforming datasets).
  Commit `ee701eb`.
- **C9.4**: Full batch of 198 × 9 = **1782 FITS + 198 mosaic PNGs**
  produced via `s4g_mock_full.sh`. Total 243 MB to
  `~/Dropbox/work/data/s4g_mock/`. Wall clock 12.2 min (wide 407.5 s
  + dud 324.7 s). New shell scripts `s4g_mock_full.sh` +
  `s4g_mock_test.sh` at repo root; existing `huang2013_full.sh` +
  `huang2013_test.sh` updated to the new runner path and committed
  (they had been untracked across multiple sessions). Commit
  `f06d30f`.

### Test suite state
- 234 passed / 5 skipped at session end.
- New tests added this session: `TestLoadImageConfigs` (6) in
  `tests/test_mockgal.py`. Existing test file renamed:
  `tests/test_generate_mocks.py` (8 tests, unchanged behaviour).

### Merged + pushed to origin/main
- Two clean fast-forwards across the day:
  1. Morning session's `feature/cs4g-sample-prep` → merged as commit
     range `052d758..9680f4c` (9 commits).
  2. Evening session's `feature/s4g-mock-production` → merged as
     `9e9f6d0..f06d30f` (4 commits).
- `origin/main` now at `f06d30f`. Both feature branches can be
  deleted locally and on the remote (not done — see "In Progress").

## In Progress (Not Finished)

### Branch cleanup
- Two merged feature branches still exist locally and on origin:
  `feature/cs4g-sample-prep` and `feature/s4g-mock-production`. Neither
  will be reused. Delete both when convenient; the commit history is
  preserved on `main`.

### Phase 10 (isophote-fitting benchmark pipeline) — NOT started
- User explicitly asked **not to implement** Phase 10 in this session.
- User has private plans and external references for the real
  benchmark and separate prep work on the `isoster` end.
- Next session: **do not touch Phase 10 without an explicit user
  request first.** Wait for instruction.

### Pre-existing dirty paths — intentionally untouched (same as morning)
- `inputs/huang2013/runs/huang2013_hsc_i_dud.yaml` (modified)
- `inputs/huang2013/runs/huang2013_hsc_i_wide.yaml` (modified)
- `.scratch/` — working dir (now also holds
  `s4g_mock_full_batch.log`, the full C9.4 production log)

These are pre-session and remain on the working tree across branch
switches.

## Problems / Blockers

None. A few things worth flagging:

- The runner's `size_factor` row-level requirement is pinned by
  `tests/test_generate_mocks.py::test_resolve_run_manifest_requires_row_level_size_factor`.
  Each CS4G row redundantly sets `size_factor: 4.0` even though it's
  already in `defaults:`. Minor cleanup candidate if ever touched.
- Multi-manifest datasets' root `run_metadata.json` only reflects the
  **last** run (dud for both Huang2013 and CS4G). Documented in
  `docs/SPEC.md` as an explicit design choice; per-FITS provenance
  is still recoverable from headers. Not a bug.
- My first P8.11 diagnosis attributed the flux excess to a cosmology
  round-trip bug — that was wrong (real cause was band mismatch).
  Committed twice: wrong diagnosis first (`3365db3` was the FIX
  commit; the original writeup in the earlier commit's lesson entry
  was wrong and got rewritten in `3365db3`). The durable lesson in
  `docs/LESSON.md` → "CS4G Band Convention" now reflects the correct
  diagnosis.

## Key Decisions

- **Phase 9 done means mocks on disk**, not a benchmark pipeline.
  Per user, Phase 9 ends at production data; Phase 10 is the
  benchmark. User has external plans for Phase 10.
- **`size_factor=4` for CS4G**, vs Huang2013's 6. Decision driven by
  profile physics: n=1 disks converge in flux 10× faster than n=4 de
  Vaucouleurs, and CS4G's max-Re component is always the disk.
  Quantitative: 98.6% disk flux captured vs Huang2013's ~88% on n=4
  de Vaucouleurs. Disk savings: 56%.
- **Mock-dataset folder contract lives in `docs/SPEC.md`**, not in
  the runner source or a single sample's README. Phase 10's benchmark
  pipeline should key off this contract, not off sample-specific
  conventions.
- **P8.12 composite PSF was deferred**, not cancelled. Composite
  PSFs are pre-fetched at `output/cs4g_s4g_irac1_test/{name}/s4g_psf_composite.fits`
  for future use. User explicit: "I can live without worrying about
  the PSF issue given the evidence."
- **Rename + lift over wrap-or-duplicate** for the production
  runner. The file-name "huang2013" was load-bearing only in one
  line (the hardcoded output subdir); everything else was already
  generic. Rename decision also drove the fix-path for the dataset
  contract.
- **Previously-untracked shell scripts were committed** in the
  Phase 9 wrap-up. `huang2013_{full,test}.sh` had been untracked
  across many sessions; after the runner rename they had to be
  edited to keep working, and leaving them untracked made the
  workflow non-reproducible from the repo.

## Branch State

- **Current branch**: `main` (synced with `origin/main` at `f06d30f`).
- **Uncommitted changes**: pre-existing dirty paths only
  (`inputs/huang2013/runs/huang2013_hsc_i_{dud,wide}.yaml` modified;
  `.scratch/` untracked).
- **Local feature branches still present**: `feature/cs4g-sample-prep`
  and `feature/s4g-mock-production`. Both merged. Safe to delete.
- **Mocks on disk**: `~/Dropbox/work/data/s4g_mock/` (198 × 9
  = 1782 FITS + 198 PNGs + 3 metadata files, 243 MB).
  `~/Dropbox/work/data/huang2013/` unchanged from the morning start
  (93 × 9 = 837 FITS, pre-existing).

## Files Modified This Session

### Created
- `inputs/cs4g/scripts/qa_mockgal_vs_s4g.py` (P8.9 validation driver)
- `inputs/cs4g/runs/cs4g_hsc_i_wide.yaml`
- `inputs/cs4g/runs/cs4g_hsc_i_dud.yaml`
- `scripts/generate_mocks.py` (renamed from
  `inputs/huang2013/scripts/generate_huang2013_mocks.py`)
- `tests/test_generate_mocks.py` (renamed from the huang2013-specific
  file)
- `docs/SPEC.md` (architectural contracts)
- `docs/plan/PLAN_CS4G_SAMPLE.md` — Phase 9 section added
- `s4g_mock_full.sh`, `s4g_mock_test.sh` at repo root
- `output/cs4g_s4g_irac1_test/qa_s4g_validation.png`,
  `qa_summary.json`, per-galaxy FITS + subcomps + composite PSFs
  (gitignored)

### Modified
- `mockgal.py` — `load_image_configs` now merges `defaults:`
- `tests/test_mockgal.py` — new `TestLoadImageConfigs` class (6
  tests); added `load_image_configs` to the import list
- `docs/LESSON.md` — rewrote "CS4G Band Convention" (replaced the
  initial-wrong "CS4G Photometric Round-Trip" entry); updated the
  "Run-Manifest Loader Behavior" entry to document current semantics
  and point at the new test class
- `docs/plan/PLAN_CS4G_SAMPLE.md` — P8.9 metrics updated post-fix,
  Phase 9 spec added
- `docs/todo.md` — rows P8.9–P8.12 marked Done/Deferred; rows C9.1–
  C9.5 added and all marked Done
- `MIGRATION.md` — path reference updated
- `huang2013_full.sh`, `huang2013_test.sh` — new runner path, added
  dataset root to `--output` (newly tracked in `f06d30f`)

### Deleted (via git mv)
- `inputs/huang2013/scripts/generate_huang2013_mocks.py` (→ `scripts/generate_mocks.py`)
- `tests/test_generate_huang2013_mocks.py` (→ `tests/test_generate_mocks.py`)

---
*Session: f54be11b-a749-4bbe-bb0c-085bb0f10a2c — resume with `claude --resume f54be11b-a749-4bbe-bb0c-085bb0f10a2c`*
