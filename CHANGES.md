# Changes Summary

This file is historical and predates the current canonical `inputs/` layout.

For current paths and workflow guidance, use:

- `inputs/README.md`
- `docs/LESSON.md`
- `MIGRATION.md`

Do not treat older references to `examples/` or a flat `inputs/` directory as current guidance.
